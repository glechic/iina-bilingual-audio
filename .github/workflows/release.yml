name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Read version
        id: version
        run: |
          VERSION=$(jq -r '.version' src/Info.json)
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Create source archives
        run: |
          tar -czf bilingual-audio-${{ steps.version.outputs.version }}.tar.gz -C . .
          zip -r -q bilingual-audio-${{ steps.version.outputs.version }}.zip . \
            -x ".git/*" "node_modules/*" "*.tar.gz" "*.zip"

      - name: Create plugin package
        run: |
          cd src && zip -r -q ../BilingualAudio.iinaplgz . && cd ..

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.version.outputs.version }}
          name: v${{ steps.version.outputs.version }}
          generate_release_notes: true
          files: |
            bilingual-audio-${{ steps.version.outputs.version }}.tar.gz
            bilingual-audio-${{ steps.version.outputs.version }}.zip
            BilingualAudio.iinaplgz